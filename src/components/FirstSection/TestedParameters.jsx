import TextWrapper from '../TextWrapper';


const TestedParameters = ({ params }) => {
  return (
    <TextWrapper $_width='100%' items={params}>Parâmetros Testados</TextWrapper>
  );
};

export default TestedParameters;
